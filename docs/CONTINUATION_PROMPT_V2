Name this chat: Job Agent Handoff 4 — Fetch + Sheet

Repo: /Users/katierobblee/Documents/job-agent

Before doing anything, read:
- README.md
- docs/HANDOFF_V2.md
- docs/HANDOFF_V1_COMPLETE.md

Current state:
- Gmail extraction works using decoded HTML body parsing
- LinkedIn job URLs extracted from <a href> links, including /comm/jobs/view/
- LLM batching and truncation are in place (MAX_JOBS_PER_LLM_CALL=10, MAX_JOB_DESCRIPTION_CHARS=6000)
- Code-fence stripping is implemented before JSON parsing
- Scoring quality is low because job-page fetching is not implemented yet

Goal for this handoff:
Implement job-page fetching with:
- per-URL timeouts
- overall run time budget
- resume via Google Sheet fetch_status
Then extract job_description, company, role_title, apply_url,
store scan-friendly summaries in the Sheet,
and only then score per the v1 contract.

Constraints:
- Do not relitigate solved decisions
- Ask if something was already solved before debugging
- One step at a time
