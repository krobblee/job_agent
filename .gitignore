# OS / editor
.DS_Store
__pycache__/
*.pyc

# Virtual env
.venv/

# Secrets / local config
.env
credentials/
gmail_credentials.json
gmail_token.json

# Local data (user-specific, never commit)
jobs.db
data/snapshots/*.json
data/profile.txt
data/feedback_raw.txt
data/learned_preferences.json
data/Startup_URLs.txt
data/Swooped_URLs.txt
LEARNING_JOURNAL.md

# Private handoff / continuation prompts (local only; not published)
docs/CONTINUATION_PROMPT*
docs/HANDOFF_*

# BFG / history cleanup
replacements.txt
bfg.jar

# Optional: logs
*.log
scripts/debug_sheet_write.py
scripts/test_write_scoring.py
